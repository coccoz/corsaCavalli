public class Cavallo {
    public run
    {
        int n;

        for(int i=0, i<100 i++)
        {
            Random random = new Random();
            n = random.nextInt(100);
            System.out.ln("il cavallo " + this + " ha percorso " + i + " metri")
            TimeUnit.MILLISECONDS.sleep(n);
        }
    }
}
